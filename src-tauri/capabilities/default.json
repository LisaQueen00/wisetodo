{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the WiseTodo main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default",
    "window-state:default"
  ]
}
